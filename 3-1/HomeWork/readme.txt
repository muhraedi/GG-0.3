1. Open the MongoDB shell in your terminal or command prompt.
2. Run the JavaScript file using the load() function in the MongoDB shell, passing the path to your JavaScript file as an argument. For example:
    load("path/to/create_database.js");
   Replace "path/to/create_database.js" with the actual path to your JavaScript file.